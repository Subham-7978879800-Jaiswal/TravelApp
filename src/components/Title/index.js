import React from 'react';
import {View, Text} from 'react-native';
import styles from './styles';

const Title = () => {
  return (
    <View>
      <Text style={styles.header}>I Got PTSD</Text>
    </View>
  );
};

export default Title;
